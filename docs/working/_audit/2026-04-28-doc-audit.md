# ドキュメント整備監査 2026-04-28

> Phase A 成果物 — 読み取り専用監査の punch list。
> 監査範囲: ① v8.2 整合性 / ② Rule 1〜5 違反検出 / ③ 索引・目次の鮮度 / ④ handoff 棚卸し
> 監査時点: v8.2.0 リリース後（TASK-0038 / PR #114 マージ済）

## サマリ

| 区分 | 件数 |
|------|------|
| Critical（必修正） | 4 |
| Major（修正推奨） | 4 |
| Minor（任意） | 4 |
| Info（判断保留） | 3 |
| False positive（対応不要） | 6 領域 |

修正推奨順序: **Critical → Major → Minor**。Info は判断後に Critical/Major へ昇格しうる。

---

## Critical（必修正）

### C-1. handoff.md 欠落（Rule 5 違反）

完了済 PBI で `handoff.md` が無いものが 3 件。

| PBI | 完了根拠 |
|-----|---------|
| TASK-0017 | PR #21 マージ済み（2026-04-19） |
| TASK-0018 | status.md に「✅ 完了」 |
| TASK-0019 | status.md に「✅ 完了」 |

修正方針: 各 TASK-XXXX 配下に `docs/working/templates/handoff.md` を雛形として handoff を生成し、必須 6 要素を埋める。完了が古いため、status.md / 当時の PR / コミット履歴から再構成する必要あり。

### C-2. `docs/ai/project-rules.md` に Orchestrator Mode 参照欠落

`docs/ai/project-rules.md:67-81` の「G. 参照先」テーブルが Orchestrator Mode を**完全に欠く**。
CLAUDE.md は Orchestrator Mode を参照（行 20）しているのに、自己宣言する正本側に記載がないため、両ファイルが宣言する「優先順位: project-rules.md（正本） > CLAUDE.md」と矛盾する。

修正方針: 「G. 参照先」テーブルに Orchestrator Mode（`docs/orchestrator-mode.md` / `.claude/rules/orchestrator-mode.md`）を追加。

### C-3. `docs/workflows/README.md` 目次に主要 3 ファイル未掲載

実体は存在するが目次に記載がない:

- `docs/workflows/execution-sequence.md`（WF-01〜WF-05 標準実行シーケンス）
- `docs/workflows/plangate-insertion-map.md`（PlanGate 既存フェーズとの対応表）
- `docs/workflows/skill-mapping.md`（Workflow phase ↔ Skill 対応マッピング）

これらは実行シーケンス定義の中核であり、発見性が著しく低下している。

修正方針: `docs/workflows/README.md` の目次テーブルに 3 行追加。

### C-4. `docs/ai-driven-development.md` のバージョン記述が v5/v6/v7 で固定

`docs/ai-driven-development.md:1,6` のタイトルと冒頭が v5/v6/v7 のみを言及し、v8.1/v8.2 への言及なし。Orchestrator Mode の分解フローへの参照も欠落。

修正方針: 冒頭バージョン表記を更新し、Orchestrator Mode への参照リンクを「関連ファイル」テーブルに追加。

---

## Major（修正推奨）

### M-1. `docs/ai/project-rules.md` のディレクトリ構造説明が v8 を反映していない

`docs/ai/project-rules.md:13-23` が v7 構造のまま。v8 で追加された `bin/plangate` CLI、RFC、`docs/schemas/` への言及なし。

### M-2. `docs/ai-driven-development.md` 「関連ファイル」テーブルの参照漏れ

`docs/ai-driven-development.md:80-end` の関連ファイル表に `docs/orchestrator-mode.md` / `docs/index.md`（存在する場合）への参照なし。v8.2 アーティファクトへのリンク整備不足。

### M-3. TASK-0036 handoff.md が参照形式のみ

`docs/working/TASK-0036/handoff.md` は「TASK-0035 に統合済み」と参照のみ。意図的な統合 handoff ならば許容できるが、本当に必須 6 要素が TASK-0035 側で完備されているかの追加確認が必要。

修正方針: TASK-0035 の handoff.md を読み、TASK-0036 のスコープが明示的にカバーされているか確認。カバーされていれば TASK-0036 側に「正本: TASK-0035/handoff.md」と明示し、誤読防止のためのクロスリンクを補強。

### M-4. Agent 定義における Rule 3 / Rule 4 境界の判断必要

監査で `.claude/agents/` 内に "PlanGate / WF-XX / TASK-XX" の言及が 24 件検出。

判断保留点:
- このリポジトリ自体が PlanGate を提供する以上、Agent 定義に PlanGate と書くのは「案件固有情報を CLAUDE.md 配下である `.claude/agents/` に寄せた結果」とも解釈できる
- ただし Agent 定義が他リポジトリへ再利用されることを想定するならば固有名を薄めるべき

→ Phase B 着手前に「方針 A: 現状維持（このリポジトリは PlanGate そのものなので OK）」「方針 B: 汎用化（PlanGate を抽象化し、固有名は CLAUDE.md/参照リンクに集約）」のどちらで進めるかをユーザー判断する必要あり。**Info → Major に昇格**。

---

## Minor（任意）

### m-1. `docs/plangate.md` の限定的な「v7 では」表現

`docs/plangate.md:62` 等で「実行層の分離 v7 では」と v7 限定で書かれている。v8 でも踏襲される前提なので「v7 以降」等に。

### m-2. `.claude/skills/README.md` に `setup-team` 未掲載

実体は `.claude/skills/setup-team/SKILL.md` として存在するが索引未記載。

### m-3. `.agents/skills/README.md` のセクション重複・順序整理

「v7 ハイブリッドアーキテクチャ対応スキル」と「汎用スキル」セクションで `brainstorming` / `self-review` / `systematic-debugging` / `subagent-driven-development` / `codex-multi-agent` が重複記載。`setup-team` は記載なし。

### m-4. CHANGELOG / testing クロスリファレンス

CHANGELOG.md で v8.1.0 以降のテストスイート 10 件への言及があるが、`docs/ai-driven-development.md` には testing 戦略への言及・リンクなし。

---

## Info（判断保留）

### I-1. `.claude/rules/orchestrator-mode.md` と `docs/orchestrator-mode.md` の二重正本

設計通り（アーキテクチャ正本 vs Gate 条件正本の分離）だが、CLAUDE.md:26 の優先順位指針「`.claude/rules/` > `.claude/skills/`」が `docs/ai/project-rules.md` に存在しない。新規利用者が参照先を判定しづらい。

### I-2. Orchestrator Mode 実装追跡表の不在

TASK-0038 配下の `plan.md` には実装追跡があるが、公開 docs（`docs/orchestrator-mode.md` 等）からは実装 PBI の追跡ができない。Spec 段階のため許容範囲だが、実装 PBI 開始時には公開側に追跡表が必要。

### I-3. M-4（Agent 内の PlanGate 参照）の方針決定

上記 M-4 と紐づく。Phase B 着手前にユーザー判断必要。

---

## False positive（対応不要、参考）

| Rule | 検出 | 判定 |
|------|------|------|
| Rule 1 | 0 件 | 違反なし |
| Rule 2 | 4 件 | すべて「プロジェクト固有という概念への言及」であり、「プロジェクト固有情報の埋め込み」ではない |
| Rule 4 重複定義 | handoff 必須化 / mode 分類 | いずれも正本宣言＋参照の構造で OK |
| 索引リンク切れ | 0 件 | OK |

---

## 修正推奨順序（Phase B プラン）

| 順序 | 区分 | 項目 | 理由 |
|------|------|------|------|
| 1 | Critical | C-2（project-rules.md 正本更新） | 他のドキュメント整合の前提 |
| 2 | Critical | C-3（workflows/README.md 目次） | 単純な追加で完結 |
| 3 | Critical | C-4（ai-driven-development.md バージョン更新） | 文言修正のみ |
| 4 | Major | M-1, M-2 | C-2 と同系統、まとめて実施 |
| 5 | Critical | C-1（handoff.md × 3 件） | 内容調査が必要なため後回し |
| 6 | Major | M-3（TASK-0036 確認） | C-1 と同時実施可能 |
| 7 | （判断要） | M-4 / I-3 | ユーザー方針決定後に着手 |
| 8 | Minor | m-1〜m-4 | バッチで実施 |

---

## 次のステップ

ユーザーは以下のいずれかを選択:

- **(a) 全件採用** — Critical 4 件 + Major 4 件 + Minor 4 件すべて修正。M-4 は方針 A/B どちらかを別途決定。
- **(b) Critical のみ採用** — C-1〜C-4 のみ修正。Major / Minor は次回。
- **(c) 個別選択** — punch list を見て ID 単位で採否決定。
- **(d) 方針確認** — M-4 / I-3 の判断軸を先に整理してから着手範囲決定。
